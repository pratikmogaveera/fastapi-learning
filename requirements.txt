# Core
fastapi[standard]==0.141.1
uvicorn==0.35.0

# Database (Phase 4)
sqlalchemy==2.0.41
asyncpg==0.30.0
alembic==1.16.4

# Auth (Phase 5)
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0

# Background tasks / queue (Phase 6)
arq==0.26.3

# Redis (Phase 9)
redis==5.2.1

# HTTP client (used in URL shortener later)
httpx==0.28.1

# Analytics parsing (used in URL shortener later)
user-agents==2.2.0

# Testing (Phase 8)
pytest==8.3.5
pytest-asyncio==0.24.0
